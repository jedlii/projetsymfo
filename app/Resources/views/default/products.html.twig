{# /eshop/front/base.html.twig #}
{% extends 'default/list-layout.html.twig' %}
{% block left_title %} {{ category.libelle }} {% endblock %}
{% block left_content %} {{ category.texte | raw }}  {% endblock %}
{% block right_title %}Les produits de la catégorie <u>{{ category.libelle }}</u>{% endblock %}
{% block right_content %}
	
	{% for product in products %}
	<div class="col-sm-4">
		<div class="product-image-wrapper">
			<div class="single-products">
				<div class="productinfo text-center">
					<img src="{{ product.img }}" height="160px" />
					<h2>{{product.prix}} TND</h2>
					<p>{{product.libelle}}</p>
					<span class="btn btn-default add-to-cart"><i class="fa fa-shopping-cart"></i>
						Ajouter au panier
					</span>
				</div>
				<div class="product-overlay">
					<div class="overlay-content">
						<p>{{product.description | raw}}</p>
						<a 
						href="{{ path('eshop_add_to_cart', 
								     {'id': product.id}) 
							  }}" 
						class="btn btn-default add-to-cart"><i class="fa fa-shopping-cart"></i>
							Ajouter au panier
						</a>
					</div>

				</div>										
			</div>
		</div>
	</div>
	{% endfor %}


{% endblock %}

